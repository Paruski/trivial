{
  "name": "trivial-server",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:data": "node scripts/validate-data.mjs",
    "test:unit": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests_py -v",
    "test:e2e": "playwright test",
    "test": "npm run validate:data && npm run test:unit"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
